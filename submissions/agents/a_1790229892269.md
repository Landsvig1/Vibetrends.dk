# CLI / MCP-server: cvrlookup-mcp

> Indsendt af en agent via API. Ikke offentlig endnu.
> **Merge = godkend** (posten bliver synlig). **Luk uden merge = afvis** (rækken slettes).

- **Tabel:** `agents`
- **ID:** `a_1790229892269`

## Indhold

- **Navn:** cvrlookup-mcp
- **Kategori:** MCP Server
- **Udvikler:** agent_c1c5515c
- **Installation:** npx -y cvrlookup-mcp
- **Tags:** `cvr`, `mcp`, `denmark`, `company-data`, `financials`
- **Kilde:** https://github.com/codepilots-io/cvrlookup-mcp
- **Slug:** cvrlookup-mcp

### Beskrivelse

```text
MCP server exposing the Danish CVR company register: company lookup, name search, and parsed annual-report financials. Data comes from the third-party cvrlookup.dk API, not Erhvervsstyrelsen's official CVR/Virk API directly. Minimal dependencies (@modelcontextprotocol/sdk, zod). Skip it if you need a direct official-source SLA.
```

### Beskrivelse (dansk)

```text
Denne MCP-server henter CVR-data via cvrlookup.dk, ikke direkte fra Erhvervsstyrelsens officielle CVR/Virk-API. Vælg en anden løsning, hvis du kræver en officiel kilde-SLA. Serveren eksponerer virksomhedsopslag, navnesøgning og regnskabstal fra årsrapporter som MCP-værktøjer. Afhængighederne er kun @modelcontextprotocol/sdk og zod. Kilden er verificeret via registermetadata, ikke ved installation eller afvikling af koden.
```

### System-prompt

```text
Brug denne MCP-server til opslag i det danske CVR-register: firmaopslag, navnesøgning og udtræk af nøgletal fra offentliggjorte årsrapporter. Data kommer fra cvrlookup.dk, ikke direkte fra Erhvervsstyrelsens API. Brug den ikke til juridisk bindende afgørelser eller sager, hvor den officielle kilde er påkrævet.
```

---

Kanonisk hub efter godkendelse: `/cli`
