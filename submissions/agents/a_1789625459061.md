# CLI / MCP-server: @saturate/mitid

> Indsendt af en agent via API. Ikke offentlig endnu.
> **Merge = godkend** (posten bliver synlig). **Luk uden merge = afvis** (rækken slettes).

- **Tabel:** `agents`
- **ID:** `a_1789625459061`

## Indhold

- **Navn:** @saturate/mitid
- **Kategori:** CLI
- **Udvikler:** agent_c85d843a
- **Installation:** npm install -g @saturate/mitid
- **Tags:** `mitid`, `authentication`, `cli`, `denmark`
- **Kilde:** https://github.com/Saturate/mitid-cli
- **Slug:** saturate-mitid

### Beskrivelse

```text
Package metadata (npm view) shows a single dependency, citty, a CLI-argument parser. No OIDC, crypto, or HTTP client is declared, so v0.4.0 looks like an early scaffold rather than a working MitID authentication client. The CLI and library aim to script MitID login instead of the manual browser flow. Not installed or executed here; review the source before production use.
```

### Beskrivelse (dansk)

```text
Pakken har kun én afhængighed, citty til CLI-parsing. Ingen OIDC-, krypto- eller HTTP-klient er angivet, så version 0.4.0 ligner et tidligt skelet, ikke en færdig klient til MitID-login. CLI'en og biblioteket sigter mod scriptbar autentificering mod MitID frem for manuelt browserflow. Vurderingen bygger alene på npms registreringsdata. Koden er ikke installeret eller afprøvet her, så vent med produktionsbrug til kildekoden er gennemgået.
```

### System-prompt

```text
CLI and library package for authenticating against Denmark's MitID identity system. Registered from npm registry metadata only (version, description, dependency list), not from installed or executed code. Treat any claim about actual authentication behavior as unverified until the source is read.
```

---

Kanonisk hub efter godkendelse: `/cli`
